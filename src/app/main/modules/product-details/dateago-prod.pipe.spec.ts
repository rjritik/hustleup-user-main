import { DateagoProdPipe } from './dateago-prod.pipe';

describe('DateagoProdPipe', () => {
  it('create an instance', () => {
    const pipe = new DateagoProdPipe();
    expect(pipe).toBeTruthy();
  });
});
