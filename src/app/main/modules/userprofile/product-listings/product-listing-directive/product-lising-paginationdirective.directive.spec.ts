import { ProductLisingPaginationdirectiveDirective } from './product-lising-paginationdirective.directive';

describe('ProductLisingPaginationdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductLisingPaginationdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
