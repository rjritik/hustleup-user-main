import { OrdershipmentPaginationDirective } from './ordershipment-pagination.directive';

describe('OrdershipmentPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new OrdershipmentPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
